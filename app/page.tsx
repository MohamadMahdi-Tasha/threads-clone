// Exporting home page (functional component) as default
export default function HomePage():JSX.Element {
    // Returning JSX
    return (
        <div>
            <h1>Home Page</h1>
        </div>
    );
}